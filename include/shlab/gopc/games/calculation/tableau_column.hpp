#pragma once

#include <cstdint>

namespace shlab::gopc::games::calculation {

enum class TableauColumn : std::uint8_t {
    First = 0,
    Second = 1,
    Third = 2,
    Fourth = 3,
};

}  // namespace shlab::gopc::games::calculation
